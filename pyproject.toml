[project]
name = "notice-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "requests",
  "pyyaml",
  "beautifulsoup4",
  "psycopg2-binary",
  "python-dotenv",
  "asyncpg",
  "aiohttp",
]
